function solution(dots) {
  var answer = 0;
  let possibleArray = [[1, 2, 3, 4], [1, 3, 2, 4], [1, 4, 2, 3]];
  
  possibleArray.map((possible) => {
    let inc1;
    let inc2;
    let first = possible[0]
    let second = possible[1]
    let third = possible[2]
    let last = possible[3]

  })

  return answer;
}